#include <iostream>

void sort3(int&a,int&b,int&c){
  int temp=a;
  if(a>b && a>c){
    a=b;
    b=temp;
  }
}

int main(){  
  int v = 3;  
  int w = 4;  
  int x = 1;    
  sort2(v, w, x); // w is now 1, x is now 4
  std::cout << u << " " << v << " " << w << " " << x << std::endl;
  return 0;
}
